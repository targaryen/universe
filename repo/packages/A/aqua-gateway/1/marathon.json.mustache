{
  "id": "{{service.name}}",
  "cpus": {{service.cpus}},
  "mem": {{service.mem}},
  "instances": {{service.instances}},
  "uris":[
    {{#docker_config.enable}}
      "{{docker_config.location}}"
    {{/docker_config.enable}}
  ],
  "acceptedResourceRoles": [
    "{{advanced.acceptedResourceRole}}"
  ],
  "constraints": [
    {{#advanced.extraConstraintName}}
      ["{{advanced.extraConstraintName}}", "LIKE", "{{advanced.extraConstraintValue}}"],
    {{/advanced.extraConstraintName}}
    ["hostname", "UNIQUE"]
  ],
  "env": {
    "SCALOCK_DBUSER": "{{database.database_user}}",
    "SCALOCK_DBPASSWORD": "{{database.database_password}}",
    "SCALOCK_DBNAME": "{{database.database_name}}",
    "SCALOCK_DBHOST": "{{database.database_host}}",
    "SCALOCK_DBPORT": "{{database.database_port}}",
    "SCALOCK_AUDIT_DBUSER": "{{database.audit_database_user}}",
    "SCALOCK_AUDIT_DBPASSWORD": "{{database.audit_database_password}}",
    "SCALOCK_AUDIT_DBNAME": "{{database.audit_database_name}}",
    "SCALOCK_AUDIT_DBHOST": "{{database.audit_database_host}}",
    "SCALOCK_AUDIT_DBPORT": "{{database.audit_database_port}}",
    "HEALTH_MONITOR": "0.0.0.0:80",
    "SCALOCK_LOG_LEVEL": "DEBUG"
  },
  "container": {
    "type": "DOCKER",
    "docker": {
      "image": "{{resource.assets.container.docker.gateway}}",
      "forcePullImage": true,
      "network": "HOST",
      "portMappings": [
        {
          "hostPort": {{service.port}},
          "containerPort": 3622,
          "protocol": "tcp"
        }
      ]
    }
  },
  "healthChecks": [
    {
      "port": 80,
      "protocol": "HTTP",
      "path": "/",
      "gracePeriodSeconds": 120,
      "intervalSeconds": 60,
      "timeoutSeconds": 30,
      "maxConsecutiveFailures": 3
    }
  ]
}
